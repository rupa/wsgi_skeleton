# My lil' WSGI skeleton for Apache.

* Subclass WSGIApp
* Write a process_request method that takes environ and returns status, headers,
  and data, e.g. ('200 OK', {'Some-Header': 'doot'}, 'Hello')
* That's pretty much it
